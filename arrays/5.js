//CODE Add th elements
function arr(n){
    var arr=[]; //Creating empty array
    for(var i=1;i<n;i++){
        arr.push(i) //Pushing all the elements in the empty array

    }
    return arr  //Returning the array were it is called

}
console.log(arr(5));


